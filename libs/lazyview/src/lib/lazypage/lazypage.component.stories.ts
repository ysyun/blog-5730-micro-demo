import { LazypageComponent } from './lazypage.component';

export default {
    title: 'LazypageComponent',
};

export const primary = () => ({
    moduleMetadata: {
        imports: [],
    },
    component: LazypageComponent,
    props: {},
});
